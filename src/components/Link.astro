---
const { href, title, label } = Astro.props;
---

<a
  href={href}
  title={title}
  aria-label={label}
  class="relative font-semibold cursor-pointer transition-all duration-300 border-b-2 border-dashed border-transparent hover:border-primary active:text-primary active:border-transparent"
>
  <slot />
</a>
